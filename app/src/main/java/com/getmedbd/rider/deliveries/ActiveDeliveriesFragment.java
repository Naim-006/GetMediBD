package com.getmedbd.rider.deliveries;

import android.os.Bundle;
import android.view.View;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.fragment.app.Fragment;

/**
 * Fragment for managing currently active deliveries assigned to the rider
 */
public class ActiveDeliveriesFragment extends Fragment {
    
    @Override
    public void onViewCreated(@NonNull View view, @Nullable Bundle savedInstanceState) {
        super.onViewCreated(view, savedInstanceState);
        // Setup active deliveries list, route navigation, and status tracking
    }
}
