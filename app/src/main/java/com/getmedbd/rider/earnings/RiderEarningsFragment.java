package com.getmedbd.rider.earnings;

import android.os.Bundle;
import android.view.View;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.fragment.app.Fragment;

/**
 * Fragment showing rider earnings, daily tips, and payout summary
 */
public class RiderEarningsFragment extends Fragment {
    
    @Override
    public void onViewCreated(@NonNull View view, @Nullable Bundle savedInstanceState) {
        super.onViewCreated(view, savedInstanceState);
        // Setup earnings summary cards, completed trips payout, and withdrawal options
    }
}
