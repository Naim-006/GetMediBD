package com.getmedbd.rider.verification;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

/**
 * Activity for completing delivery with OTP verification, photo proof, or customer signature
 */
public class ProofOfDeliveryActivity extends AppCompatActivity {
    
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Setup OTP input, photo capture for delivered parcel, and customer signature
    }
}
