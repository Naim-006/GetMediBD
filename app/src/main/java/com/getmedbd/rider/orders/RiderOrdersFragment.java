package com.getmedbd.rider.orders;

import android.os.Bundle;
import android.view.View;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.fragment.app.Fragment;

/**
 * Fragment for browsing, accepting, or rejecting incoming delivery orders
 */
public class RiderOrdersFragment extends Fragment {
    
    @Override
    public void onViewCreated(@NonNull View view, @Nullable Bundle savedInstanceState) {
        super.onViewCreated(view, savedInstanceState);
        // Setup incoming order delivery requests and pickup requests
    }
}
